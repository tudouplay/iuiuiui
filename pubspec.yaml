name: moon_tv
description: 影视聚合播放应用

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  provider: ^6.1.1
  cached_network_image: ^3.3.0
  video_player: ^2.8.2
  chewie: ^1.5.0
  pull_to_refresh: ^2.0.0
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.1
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/